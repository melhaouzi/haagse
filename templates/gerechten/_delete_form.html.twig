<form method="post" action="{{ path('gerechten_delete', {'id': gerechten.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gerechten.id) }}">
    <button class="btn">Delete</button>
</form>

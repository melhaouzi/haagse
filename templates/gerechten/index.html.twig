{% extends 'base.html.twig' %}

{% block title %}Gerechten index{% endblock %}

{% block body %}
    <h1>Gerechten index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Soort_gerecht</th>
                <th>Naam</th>
                <th>Prijs</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for gerechten in gerechten %}
            <tr>
                <td>{{ gerechten.id }}</td>
                <td>{{ gerechten.soortGerecht }}</td>
                <td>{{ gerechten.naam }}</td>
                <td>{{ gerechten.prijs }}</td>
                <td>
                    <a href="{{ path('gerechten_show', {'id': gerechten.id}) }}">show</a>
                    <a href="{{ path('gerechten_edit', {'id': gerechten.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('gerechten_new') }}">Create new</a>
{% endblock %}
